#include "parser.h"
#include <stdio.h>

int main(int argc, char const* argv[])
{
    //TESTING ONLY
    // stdin = fopen("../tests/parserTestSources/basic/basic4.go", "r");
    parser_start();
    // fprintf(stderr, "SUCC\n");
    return 0;
}
